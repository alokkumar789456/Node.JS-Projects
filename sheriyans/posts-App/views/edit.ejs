<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom font */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
        }

        .hover-transition:hover {
            transition: background-color 0.3s ease, color 0.3s ease;
        }
    </style>
</head>

<body class="bg-gradient-to-r from-gray-800 via-gray-900 to-black text-white">
    <div class="p-6 max-w-4xl mx-auto">
        <div class="mb-4">
            <a href="/logout" class="text-blue-400 hover:text-blue-300 hover-transition">Logout</a>
        </div>
        <h5 class="text-xl mb-6">You can edit your Post...</h5>
        <form action="/update/<%= post._id%>" method="post" class="bg-gray-800 p-4 rounded-lg shadow-lg">
            <textarea placeholder="What's on your Mind?" name="content"
                class="w-full h-32 p-2 border border-gray-700 bg-gray-900 text-white rounded-md focus:ring-2 focus:ring-blue-600 transition-all"><%= post.content %></textarea>
            <input type="submit" value="Update Post"
                class="mt-4 bg-blue-600 text-white p-2 rounded-md cursor-pointer hover:bg-blue-500 transition-all">
        </form>
    
    </div>
</body>

</html>