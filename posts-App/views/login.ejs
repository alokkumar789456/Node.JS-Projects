<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>POST-APP</title>
    <style>
        /* Custom font */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
        }
        .hover-transition:hover {
            transition: background-color 0.3s ease, color 0.3s ease;
        }
    </style>
</head>

<body class="bg-gradient-to-r from-gray-800 via-gray-900 to-black flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 p-6 rounded-lg shadow-lg w-full max-w-sm">
        <div class="mb-4">
            <a href="/" class="mr-10 text-blue-400 hover:text-blue-300 hover-transition">Create Account</a>
            <a href="/profile" class="mr-10 text-blue-400 hover:text-blue-300 hover-transition">Profile</a>
        </div>
        <h3 class="text-xl font-semibold mb-4 text-white">Login Account</h3>
        <form action="/login" method="post" class="space-y-4">
            <div class="flex gap-4">
                <input type="email" name="email" placeholder="Email"
                       class="border border-gray-700 p-2 rounded-md flex-1 bg-gray-900 text-white focus:ring-2 focus:ring-blue-600 transition-all">
            </div>
            <div class="flex gap-4">
                <input type="password" name="password" placeholder="Password"
                       class="border border-gray-700 p-2 rounded-md flex-1 bg-gray-900 text-white focus:ring-2 focus:ring-blue-600 transition-all">
            </div>
            <input type="submit" value="Login Account" class="bg-blue-600 text-white p-2 rounded-md cursor-pointer hover:bg-blue-500 transition-all">
        </form>
    </div>
    <script src="/javascript/script.js"></script>
</body>
</html>
